# Plan

1. Bootstrap a React + Vite app because the project has no existing framework.
2. Build a left-edge Codex Use drawer based on the Tado Use pattern: chat, engine badge, tool-call rows, local run state, and close/open affordance.
3. Add a rule-based router that maps engineering requests to pipeline stages, agent packages, and skills.
4. Add the main workspace: plan preview, output contracts, stage status, package registry, and run history.
5. Document the agent-pipeline handoff prompts so future sessions can continue without hidden chat context.

